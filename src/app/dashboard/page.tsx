import { redirect } from "next/navigation";
import { getSession } from "@/lib/auth";

export default async function Dashboard() { const session = await getSession(); if (!session) redirect("/login"); return <main className="app-shell"><nav className="topbar"><div className="brand-mark">H<span>.</span></div><div className="nav-user"><span className="avatar">{session.name[0]}</span><span>{session.name}</span><a href="/api/auth/logout">Sair</a></div></nav><section className="dashboard-content"><div className="welcome"><div><p className="eyebrow muted">PAINEL DO ALUNO / 01</p><h1>Olá, {session.name.split(" ")[0]}.</h1><p>Seu espaço de evolução está pronto para continuar.</p></div><div className="status-pill"><i /> ACESSO ATIVO</div></div><div className="hero-card"><div><p className="eyebrow">FERRAMENTA PRINCIPAL</p><h2>Crie sua retícula.</h2><p>Converta sua arte para uma retícula pronta para DTF com controle de LPI, ângulo e DPI.</p><a className="button button-light" href="/halftone">Abrir Halftone Studio <span>↗</span></a></div><div className="hero-number">01</div></div><div className="section-heading"><h3>Seu espaço</h3><span>ATUALIZADO AGORA</span></div><div className="dashboard-grid"><article className="module-card"><span className="card-index">01 / HALFTONE</span><h3>Gerador de retícula</h3><p>Prepare imagens para impressão com preview local e exportação de alta resolução.</p><a className="text-link" href="/halftone">Abrir ferramenta ↗</a></article><article className="module-card warm"><span className="card-index">02 / PRÓXIMO</span><h3>Laboratório DTF</h3><p>Novos módulos de separação de cores e preparação de arquivos em breve.</p><span className="text-link">Em construção</span></article><article className="quote-card"><p>“A consistência é uma decisão que você toma todos os dias.”</p><span>HALFTONE NOTE / 002</span></article></div></section></main>; }