"use client";
import { FormEvent, useState } from "react";

export default function LoginPage() {
  const [error, setError] = useState(""); const [loading, setLoading] = useState(false);
  async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setLoading(true); setError(""); const data = Object.fromEntries(new FormData(event.currentTarget)); const response = await fetch("/api/auth/login", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(data) }); if (response.ok) window.location.href = "/dashboard"; else { setError((await response.json()).error || "Não foi possível entrar."); setLoading(false); } }
  return <main className="auth-shell"><section className="auth-art"><div className="grain" /><p className="eyebrow">HALFTONE / MASTERY CLUB</p><h1>Transforme<br /><em>conhecimento</em><br />em movimento.</h1><p className="art-copy">Um espaço para aprender, praticar e construir a próxima versão do seu trabalho.</p><div className="orbit orbit-one" /><div className="orbit orbit-two" /><span className="vertical-label">EST. 2024 · ONLINE LEARNING</span></section><section className="auth-panel"><div className="brand-mark">H<span>.</span></div><div className="form-wrap"><p className="eyebrow muted">ÁREA DO ALUNO</p><h2>Bem-vindo de volta.</h2><p className="subhead">Entre para continuar de onde parou.</p><form onSubmit={submit}><label>E-mail<input name="email" type="email" placeholder="voce@email.com" required /></label><label>Senha<input name="password" type="password" placeholder="Sua senha" required /></label>{error && <p className="error">{error}</p>}<button className="button button-dark" disabled={loading}>{loading ? "Entrando..." : "Entrar na plataforma"}<span>↗</span></button></form><div className="divider"><span>AINDA NÃO É ALUNO?</span></div><a className="button button-outline" href="/checkout">Quero começar agora <span>↗</span></a><p className="fine-print">Ao entrar, você concorda com nossos termos de uso.</p></div></section></main>;
}