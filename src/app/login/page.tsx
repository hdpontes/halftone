"use client";
import { FormEvent, useState } from "react";

export default function LoginPage() {
  const [error, setError] = useState(""); const [loading, setLoading] = useState(false);
  async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setLoading(true); setError(""); const data = Object.fromEntries(new FormData(event.currentTarget)); const response = await fetch("/api/auth/login", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(data) }); if (response.ok) window.location.href = "/dashboard"; else { setError((await response.json()).error || "Não foi possível entrar."); setLoading(false); } }
  return <main className="auth-shell"><section className="auth-art"><div className="grain" /><p className="eyebrow">HPBOOST / DTF TOOLS</p><h1>Arte pronta.<br /><em>Impressão</em><br />precisa.</h1><p className="art-copy">Ferramentas profissionais para quem transforma imagem em estampa.</p><div className="orbit orbit-one" /><div className="orbit orbit-two" /><span className="vertical-label">HALFTONE ENGINE · 01</span></section><section className="auth-panel"><div className="brand-mark">H<span>.</span></div><div className="form-wrap"><p className="eyebrow muted">ACESSO À PLATAFORMA</p><h2>Entrar.</h2><p className="subhead">Abra suas ferramentas de produção.</p><form onSubmit={submit}><label>E-mail<input name="email" type="email" placeholder="voce@email.com" required /></label><label>Senha<input name="password" type="password" placeholder="Sua senha" required /></label>{error && <p className="error">{error}</p>}<button className="button button-dark" disabled={loading}>{loading ? "Verificando..." : "Entrar nas ferramentas"}<span>↗</span></button></form><div className="divider"><span>AINDA NÃO TEM ACESSO?</span></div><a className="button button-outline" href="/checkout">Assinar acesso mensal <span>↗</span></a><p className="fine-print">Workspace profissional para DTF e estamparia.</p></div></section></main>;
+}
